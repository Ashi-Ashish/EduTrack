/**
 * API type definitions and error classes.
 * Response types, error handling, and validation structures.
 * 
 * @module api/types
 */

export { ApiError } from './errors';
export type { ValidationError } from './errors';
export type { PaginatedResponse, ApiResponse } from './responses';
